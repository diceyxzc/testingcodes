#two decimal places

length = float(input("Enter length of a rectangle: "))
width = float(input("Enter the width of the rectangle: "))

area = length * width

print(f"{area:.2f} is the equivalent area of the inputted measurements.")